{
    "enable": false,
    "Parameters": {
        "webMarket": "en-uk"
    },
    "Items": [
        {
            "Name": "{{config.name}}bing",
            "ResourceGroup": "{{config.resourceGroupName}}"
        }
    ]
}